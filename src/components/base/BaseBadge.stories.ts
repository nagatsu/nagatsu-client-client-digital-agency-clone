import BaseBadge from './BaseBadge.vue'

export default {
  title: 'Base/BaseBadge',
  component: BaseBadge,
}

export const Primary = {
  args: {
    label: 'Primary',
    variant: 'primary'
  }
}

export const Success = {
  args: {
    label: 'Success',
    variant: 'success'
  }
}
